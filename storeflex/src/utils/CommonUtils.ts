export const CHARACTER_ONLY = /^[A-Za-z ,.'-]+$/;

export const validatetName = (minLen: number, maxLen: number) => {
    console.log(' validatetName : minLen  and maxLen', minLen, maxLen);
}

export const validateEmail = (minLen: number, maxLen: number) => {
    // console.log('  ')
}