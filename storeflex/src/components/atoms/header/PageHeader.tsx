import React, { Component } from 'react';

const PageHeader = () => {

    return(
        <header className='app-header'>
            <h1> Page Header</h1>
        </header>
    );
}

export default PageHeader;